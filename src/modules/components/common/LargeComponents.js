import { Card, styled } from "@mui/material";

export const LargeComponents = styled(
  Card,
  {}
)(() => ({
  width: "1200px",
  display: "flex",
  margin: "0 auto",
  position: "relative",
  zIndex: 1,
  overflow: "unset",
}));
