import { Card, styled } from "@mui/material";
// cards to render center field content
export const StyledCard = styled(
  Card,
  {}
)(() => ({
  width: "670px",
  display: "flex",
  margin: "0 auto",
  padding: "15px 20px",
}));
